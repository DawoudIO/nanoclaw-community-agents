{
  "image_digest": null,
  "release": "v2.3.0",
  "_note": "image_digest was the sbx-kit digest from before this deployment moved to running nanoclaw.sh directly (see UPSTREAM-ISSUES.md #15) -- it described a kit image this deployment no longer pulls, not the agent container image, so it was cleared rather than left to look like current verified data. Set it once you've verified an agent image digest (versions.json's agent-image field, or the hardened-image registry) against this template set."
}
