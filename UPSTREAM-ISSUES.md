# Upstream issue tracker — candidates for nanocoai/nanoclaw

Platform behaviors observed on real installs that likely affect every
NanoClaw user. Rule: **confirm on a clean install first** (`nanoclaw.sh`,
current `main` — this deployment no longer uses `sbx`, see #15/#16 for why),
then file — one issue each, include the image digest from
`platform-baseline.json`, reference existing issue #411 where relevant.
Update the Status column as testing progresses.

Entries #11, #13, #15, #16 below are about the `sbx` Docker-Sandboxes
deployment path specifically. That path was abandoned (#15/#16 are why) —
kept as the historical record, closed rather than deleted, since they're
the actual evidence for that decision, not stale TODOs.

| # | Candidate issue | Type | Status |
|---|---|---|---|
| 1 | Message IDs are not one monotonic sequence across messaging groups / directions, but nothing documents their semantics — agents (and humans) misread cross-sequence comparisons as forgery evidence | docs/bug | observed old install; confirm on clean |
| 2 | Task-shaped prompts fire with no backing task session (`append-log` → "no task session to derive it from"; series absent from `ncl tasks list`) — phantom re-fires of cancelled/stale tasks, or an injection surface | bug (serious) | observed 3× in one morning; try repro: cancel a recurring task, watch subsequent slots |
| 3 | Task rows mutate without an acting session: cancelled series recreated as a new live row (`-3362` → `-6414`), `origin_session_id` pointing at a session with no `messaging_group_id`/`thread_id`; no audit trail on task-table writes | bug (serious) | observed; needs admin-level repro |
| 4 | No session attribution on outbound messages: multiple sessions of one agent DM the owner indistinguishably — the root of a day-long false "impersonation" incident. Feature: origin tag per message (e.g. "via task <series>") | feature | confirmed by design; file as enhancement |
| 5 | Concurrent sessions share one group workspace with no write attribution — file edits/staging appear "between two git status calls" of another session; agents read sibling writes as tampering. Feature: per-session write audit log or advisory attribution | feature/bug | observed; confirm with two parallel sessions on clean install |
| 6 | Context reset/compaction happens mid-conversation without notifying the agent or owner ("missing memory of a chunk of today's session") | bug/docs | observed once |
| 7 | Docs don't state what's available inside task `script:` gates — which binaries ship in the agent image (`bash`, `curl`, `jq`?) and whether `ncl` is callable from a script | docs | our health-check gate now self-reports missing jq/ncl (once) and the integrity gate falls back to a manual wake — if either fires on the clean install, file this with that evidence |
| 8 | All `ncl tasks` verbs open to agent callers regardless of `cli_scope` — scheduled tasks are the persistence vector in published injection research; feature: task-mutation approval gate or prompt integrity (hash) | security/feature | per public docs + embracethered research; reference issue #411 |
| 9 | Discord adapter: raw URLs and markdown links render unclickable; proactive rich cards need a two-step send_message→send_card dance | bug | observed old install; confirm on clean |
| 10 | Manually-created messaging-group destinations silently fail outbound (no Discord adapter association) — should error loudly instead | bug | observed old install; confirm on clean |
| 11 | sbx kit: default network allowlist lacked common analytics hosts (GA4 `analyticsdata.googleapis.com`, PostHog, Gmail) | docs (file on docker/sbx-kits-contrib) | **closed — moot.** This deployment no longer uses the sbx kit at all (see #15/#16); not upstream-fileable against a path we don't run |
| 12 | No documented tool for an agent to construct its own clickable Approve/Reject card for an arbitrary decision (only the built-in self-mod `install_packages`/`add_mcp_server` and OneCLI credential flows get real approval buttons, per `src/modules/approvals/`) — worth an upstream ask, since it would let judgment-call approvals (not just outbound-API-backed ones) use real buttons too | feature | confirmed absent from the approvals module's docs; verify no agent-facing equivalent exists on the clean install before filing |
| 13 | No documented upgrade path for sbx-kit installs. Also: no export/import for channel wirings, which makes recreate tedious on servers with many channels | docs/feature | **the sbx-kit half is closed — moot** (see #15/#16); the channel-wiring export/import gap is real regardless of deployment path and still worth filing |
| 14 | **Is a per-group timezone override supported for task schedules, and does it actually apply to cron?** The kit pins `TZ=UTC`; three of our template READMEs originally claimed schedules "fire in the group's configured timezone," which nobody could verify. This matters a lot: if it works, a mis-timed schedule is a config change; if it doesn't, it's cancel-and-recreate for every task. Needs a documented answer either way | docs | claim removed from our docs pending verification — ask upstream, or test by setting a group timezone and confirming a task fires at the expected local time |
| 15 | **`spec.yaml` format rejected by the sbx CLI/runtime as incompatible** on a real install attempt (Mac mini, 2026-08-22) against the pinned kit (`docker.io/sbx/nanoclaw-kit@sha256:3a6f8f9...`, release `v2.3.0`, `platform-baseline.json`'s `kit_commit` `1f2f622`). **Docker Desktop and the `sbx` CLI were both updated to latest immediately before this attempt** — rules out "stale sbx version" as the cause; points to a real incompatibility between current sbx and this kit build (or a genuine sbx-side regression). Forced abandoning sbx for a native (non-sandboxed) install. **NOT YET FILEABLE** — nothing beyond this one-line summary was captured at the time: no exact error text, no `sbx --version` output, no diff of which `spec.yaml` field/key was rejected. Needs, before filing: the exact CLI output (terminal scrollback or a fresh repro), `sbx --version` / Docker Desktop version actually used, and confirmation the same `spec.yaml` in this repo is what was actually run | bug — **repo unclear, ask upstream which team owns the `sbx` CLI/runtime vs. kit-content schema** (kit-content issues go to `docker/sbx-kits-contrib` per #11; this may be the sbx CLI itself, a different repo — `nanocoai/nanoclaw` is confirmed real and active, so check there first if unsure) | summary only, no repro captured — see notes above |
| 16 | **Docker storage errors on image unpack** when pulling/starting the sbx kit, same install attempt as #15, same latest-Docker/latest-sbx context. **NOT YET FILEABLE** — same gap: no exact error text, exit code, or storage-driver detail captured. Needs: the literal error output, `docker info` (storage driver), and whether it reproduces on a fresh `docker system prune` + re-pull | bug — same repo question as #15 | summary only, no repro captured |
| 17 | **Outbound message double-posting to Discord — mostly explained, one theory not it, and a real upstream fix already exists (unmerged).** The weekend install (2026-08-22/23) hit this repeatedly and it self-diagnosed correctly one time: the agent explicitly admitted "I called the message-send tool mid-turn and then also produced a final wrapped reply with the same text — both genuine separate deliveries... a mistake in how I've been operating this turn, not a platform glitch." Template-level mitigation applied (an explicit "exactly one delivery per reply" rule in the lead's `instructions.md`). **Confirmed 2026-08-24: this exact bug already has an open, unmerged PR from our own fork** — [`DawoudIO/nanoclaw` PR #3462](https://github.com/nanocoai/nanoclaw/pull/3462), "guard send_message against re-sending content the mid-turn block door already delivered," which fixes the platform-level root cause (the `send_message` MCP tool had no echo-guard against content the mid-turn block door already sent; `<message>` blocks did). References #2404 for the other, still-open direction of the same bug class. A separate, unrelated code-level race was also found while investigating (`src/delivery.ts`'s retry-without-idempotency-key path) — real and confirmed against source, but no evidence it's what actually happened here; leave open as a second possible contributor | bug — real fix exists, unmerged: `nanocoai/nanoclaw` PR #3462 | template fix applied 2026-08-24; PR #3462 open and unmerged upstream — watch its status, and watch tonight's fresh install to confirm the template fix holds regardless |
| 18 | **Intermittent, unrepairable task database corruption on a stamped group.** Weekend install (2026-08-22): local-ops' task database repeatedly went into a state where `ncl tasks list`/`ncl tasks run` failed against it — clearing on its own at times, then re-corrupting mid-session, sometimes right after retrying a trigger. Neither the affected agent nor the lead could locate the actual database file from inside their sandboxes to attempt a repair. **A strong candidate mechanism was found in `nanocoai/nanoclaw`'s own source (2026-08-24)**: the central DB (`src/db/connection.ts`, where task/schedule state lives) is opened in WAL mode with no `busy_timeout` and none of the three cross-mount protections the *same codebase* already had to add to its per-session DBs after hitting this identical class of bug there (`src/session-manager.ts`'s documented invariants: `journal_mode=DELETE`, disabled mmap, `busy_timeout=5000`, confirmed applied in `container/agent-runner/src/cli/ncl.ts`). Two candidate causes, not mutually exclusive: an ungraceful process shutdown mid-WAL-write (the most common real-world cause of exactly this "corrupts, clears, corrupts again" pattern), or lock contention misread as corruption due to the missing `busy_timeout`. **NOT YET FILEABLE** — no exact error text captured, no confirmation of which mechanism, and the DB's exact location for this install was never confirmed. Needs: follow `docs/DB-HEALTH-CHECK-RUNBOOK.md` immediately if this recurs (before touching anything) — it runs a safe, read-only `PRAGMA integrity_check` per DB via `scripts/db-health-check.sh` and reports whether it's genuine corruption vs. just a stale WAL, without risking further damage, plus what to capture for this entry | bug (serious) — likely `nanocoai/nanoclaw`'s `src/db/connection.ts`, missing the hardening `src/session-manager.ts` already has | recurred multiple times over one evening, self-resolved each time; runbook ready to follow at first sign of recurrence tonight |
| 19 | **OneCLI gateway briefly failed to resolve routing rules for ALL GitHub reads, across every repo at once** — not a per-repo or per-token problem. Weekend install (2026-08-23, ~13:14–14:48 PT): `github-first-response`'s gate reported partial-fetch-failure across all four mapped repos, and a direct ad-hoc read on a repo that had worked fine an hour earlier failed with "OneCLI gateway failed to resolve rules for this request." Cleared on its own roughly 90 minutes later with no host-side intervention observed. While it lasted, every GitHub-API-dependent agent (lead, Reviewer) was simultaneously blind to new issues/PRs — a single point of failure for the whole system's GitHub-facing half. **This is the strongest real-world argument for the shared local git-mirror design** (2026-08-24 session): a filesystem-mounted mirror maintained by local ops would have kept code-reading work (Reviewer's `dependabot-pr-review`, `docs-currency-watch`) alive through this exact outage, even though live issue/PR triage inherently still needs the API. **NOT YET FILEABLE** — no host-side OneCLI gateway logs were captured during the outage, and it's unclear whether this was a proxy bug, a transient upstream GitHub API issue the gateway mis-surfaced, or a local resource/config issue. Needs: gateway logs from the outage window, and whether it correlates with anything in `docker stats`/`docker logs` for the gateway container | bug — proxy/gateway layer, exact repo unclear (`nanocoai/nanoclaw` OneCLI integration, or OneCLI itself) | one observed ~90-minute outage, self-resolved; watch for recurrence and capture gateway-side logs if it happens again |
| 20 | **Discord approval-card buttons: clicking Approve gets treated as Deny — two independent, unmerged fixes for the same bug.** Root cause: the button is built with both `id` and `value` set to the same option index; the Discord chat-adapter concatenates them with `\n`, corrupting `custom_id` (e.g. `"ncq:<questionId>:0\n0"`), and the resolver's numeric-only guard then fails, so the raw corrupted value propagates and gets treated as anything-but-`'approve'` → deny. Confirmed live by two independent parties. **Two open, unmerged PRs**: our own fork's [`#3458`](https://github.com/nanocoai/nanoclaw/pull/3458) (fixes upstream of the bug — drops the redundant `value` param so the `\n` is never created) and a third party's [`#3185`](https://github.com/nanocoai/nanoclaw/pull/3185) (fixes downstream — strips the `\n` before resolving). Related: issues [`#3456`](https://github.com/nanocoai/nanoclaw/issues/3456) and [`#3201`](https://github.com/nanocoai/nanoclaw/issues/3201) (same bug, user-reported). #3185 also notes an unfixed follow-on: a *failed* resolution still strips the buttons off the card (`components: []` sent regardless of outcome), permanently bricking it since `pending_channel_approvals` then blocks a reissue as "already in flight." **Only known mitigation, not a fix**: the bug is specific to the HTTP webhook fallback path — the Gateway path decodes correctly. Keeping Discord's Message Content privileged intent enabled (already required by this template set for auto-reply) keeps interactions on the working Gateway path. Not proactively tested as a full workaround — confirm the Gateway path is actually what's live before relying on it | bug — real fixes exist, unmerged: `nanocoai/nanoclaw` PR #3458 (ours) and #3185 (third-party) | both PRs open; watch for either to merge, and watch whether any approval card gets rejected on the first click during tonight's install (would indicate the webhook fallback is active, not Gateway) |
| 21 | **No pre-approval or batch-approval mechanism for `access: 'approval'` CLI actions.** Confirmed by reading the platform's own guard/grant code (`src/cli/guard.ts`, `src/guard/guard.ts`): a "grant" only exists after a human has already approved that exact request via a live card, is bound to it, and is deleted on use — there is no config, allowlist, or policy table that lets an operator pre-authorize a known-safe repeated action (e.g., stamping a vetted template) ahead of time. Practical effect on this template set: stamping three sub-agents (plus `jq` installs) surfaces one real approval card **per action**, not one combined card, no matter how the owner is asked in conversation — our own `welcome/SKILL.md` used to imply otherwise and was corrected 2026-08-24 to set that expectation accurately. Worth a real upstream feature request: a way to mark a specific, narrowly-scoped action (e.g. "stamp from template X") as pre-approved for a bounded window, without weakening the guard for anything else | feature | confirmed absent from the guard/grant code; not yet filed upstream |
| 22 | **Rate-limited turns flood the channel with duplicate error notices — no backoff/dedup on `deliverErrorResult`.** Real install (2026-08-27) produced thousands of duplicate "session limit · resets HH:MM" messages to one channel over ~1 hour. Confirmed against source: `providers/claude.ts` classifies a rejected `rate_limit_event` (`classification: 'rate_limit'\|'quota'`) but that `error`-type `ProviderEvent` is never consumed — `poll-loop.ts`'s main event loop has no `else if (event.type === 'error')` branch, and the terminal `result` variant that delivery actually acts on has no `classification` field at all, so the distinction is structurally discarded by delivery time. `deliverErrorResult` (poll-loop.ts ~602-607) then fires unconditionally on every completed error turn, with no memory of the identical notice it just sent. `host-sweep.ts`'s real exponential backoff only covers stuck/crashed containers — a rate-limited turn completes cleanly (`markCompleted` fires before dispatch), so it never engages. Template-level mitigation applied same day: added a persona rule to `opensource/community-manager/.../context/instructions.md` (owner-DM-only, one notice per incident, state the reset time or say plainly it's missing, queue-don't-drop) — correct but **insufficient alone**, since the flood originates from unconditional host-code delivery outside the agent's own turn/decision-making, so no system-prompt instruction can intercept it | bug (serious) — `nanocoai/nanoclaw`, `container/agent-runner/src/poll-loop.ts` + `providers/claude.ts` | **filed**: [`nanocoai/nanoclaw#3576`](https://github.com/nanocoai/nanoclaw/issues/3576) |
| 23 | **"Choose an agent" picker prompts on every new channel, even with only one eligible agent-group.** Real install (2026-08-27): owner had to manually pick the lead agent every single time a new Discord channel was mentioned, though sub-agents are never wired to public channels. Confirmed via source: `src/modules/permissions/channel-approval.ts`'s `requestChannelApproval` already special-cases a single visible agent-group in its button labeling (`buildApprovalOptions`), but always sends the card regardless; no `default_agent`/`auto_wire` setting exists anywhere in config | feature | **filed**: [`nanocoai/nanoclaw#3577`](https://github.com/nanocoai/nanoclaw/issues/3577) |
| 24 | **Discord `--platform-id` documented as the bare channel snowflake — should be `discord:<guild_id>:<channel_id>`.** Real install (2026-08-27): all 11 channels were wired using the bare snowflake per `welcome/SKILL.md`'s own instructions, so none matched real incoming messages; only caught by manual test-message verification, requiring a full tear-down and re-wire. **Template fix applied same day**: `opensource/community-manager/skills/welcome/SKILL.md` §5c now documents the composite format and how to obtain the guild ID (read it off an auto-created destination, or ask the owner for "Copy Server ID"). Template-only — not upstream-fileable, since the platform's `--platform-id` behavior is correct; the bug was our own doc | bug (docs, ours) | fixed in `welcome/SKILL.md` 2026-08-27 |
| 25 | **Release-announcement task never cross-referenced `discord-mechanics.md`'s "no markdown links" rule.** `release-announcement-watch.md` told the agent to use a card button (correct mechanism) but didn't name the rule or the doc, and only described one button — a proactive announcement with two links (release notes + changelog) was posted as plain markdown links instead, which the owner flagged directly. **Template fix applied same day**: added an explicit cross-reference and a note to use a second button for the changelog/compare link when available | bug (docs, ours) | fixed in `release-announcement-watch.md` 2026-08-27 |
| 26 | **No structured way to know a task run failed specifically due to rate-limiting, and no retry-by-run-id.** Real install (2026-08-28): owner wants a job that finds tasks that failed from hitting the shared usage window and re-triggers them once it resets — for an infrequent (e.g. weekly) task, waiting for the next natural schedule after a rate-limited failure is a real gap. Confirmed via source: the `rate_limit`/`quota` classification computed in `providers/claude.ts` is discarded before reaching any durable store (same root cause as #22/row above) — `getTaskStats()`/the `messages_in` table only track a bare `failed_runs` count, no reason field; the run log is free text, not structured; and `src/cli/resources/tasks.ts`'s command registry has no retry-by-run-id, only `ncl tasks run <id>` which re-fires the series' current template as a new one-shot, with no memory of what a specific prior run actually failed on. Not buildable as a template task without this | feature | **filed**: [`nanocoai/nanoclaw#3599`](https://github.com/nanocoai/nanoclaw/issues/3599) |
| 27 | **Coding sub-agent's PR-drafting pipeline can commit a file's raw base64 encoding as its literal content, instead of the decoded source.** Real install (2026-08-28): a PR meant to swap `Family::getFamilyString()` for the family name in the Anniversary calendar (fixing a confidentiality-adjacent display bug, [ChurchCRM/CRM#9577](https://github.com/ChurchCRM/CRM/issues/9577)) replaced the entire target file with its own base64-encoded text as the file content — decodes back to the *correct* fix, but as committed the file is unparseable PHP, a hard fatal error if merged. Caught before merge (PR closed, unmerged: [ChurchCRM/CRM#9580](https://github.com/ChurchCRM/CRM/pull/9580)). Root cause is almost certainly a decode/encode-boundary slip around GitHub's Contents API, which base64-encodes file content on both `GET` and the write side — nothing in `community-coding`'s docs warned about this boundary at all (confirmed: zero mentions of base64/decode/encode anywhere in that template before today). **Template fix applied same day**: added explicit decode-before-edit/encode-before-write guidance, with a "read back what you just wrote if in doubt" check, to `community-coding/ai.nanoco.nanoclaw/context/instructions.md`. Template-only for now — worth a closer look at exactly which call in the coding agent's actual tool use produced this before ruling out a platform-side contributor (e.g. a tool that hands back Contents-API JSON verbatim without an obvious decode step) | bug (serious) — likely ours (prompt/tooling), possibly `nanocoai/nanoclaw` if a built-in GitHub tool is the one leaking the encoding boundary | template fix applied 2026-08-28; not yet root-caused to a specific tool call — watch for recurrence |
| 28 | **`ncl destinations create`/`remove` don't auto-fill `--agent-group-id` like every other group-scoped write command.** Real install (2026-08-27 to 09-01, owner's own system review): the approval card succeeds, then execution fails with a bare `--agent-group-id is required`, costing a round-trip every time. Confirmed via source: `src/cli/resources/tasks.ts`'s `groupArg(args, ctx)` auto-fills from the caller's own scope for every task command; `src/cli/resources/destinations.ts`'s `create`/`remove` read `args.agent_group_id` directly with no equivalent, and don't say it's required anywhere the approval flow would show | bug — `nanocoai/nanoclaw`, `src/cli/resources/destinations.ts` | **filed**: [`nanocoai/nanoclaw#3699`](https://github.com/nanocoai/nanoclaw/issues/3699) |
| 29 | **Destination local-names don't repoint when their target messaging-group is recreated; outbound send reports success against a dead target.** Real install (2026-08-27 to 09-01): after recreating a messaging-group to fix its platform-id, the existing destination kept pointing at the old dead id — `send_message` reported success while nothing reached Discord, which is how the first 7.6.2 release announcement silently failed to post. Related to but distinct from #2423 (general silent-swallow of delivery failures): this is a send reaching a *stale but resolved* target, not a failure to reach a valid one | bug (serious) — `nanocoai/nanoclaw` | **filed**: [`nanocoai/nanoclaw#3700`](https://github.com/nanocoai/nanoclaw/issues/3700) |
| 30 | **`github-first-response`'s gate swallowed HTTP errors via bare `curl -fsS`, producing indistinguishable "degraded repo" escalations** — a genuine break, a transient 5xx, a 429 rate limit, and a 401 all looked identical, with no status code reaching the report. **Template fix applied 2026-09-01**: switched to the `-w '\n%{http_code}'` + tail/sed split pattern already used elsewhere in this repo (`release-announcement-watch.sh`), and carry the actual status/reason into `degraded_repos`. Template-only, not upstream-fileable — this was our own gate script | bug (ours) | fixed in `github-first-response.sh` 2026-09-01 |
| 31 | **`owner-tldr`'s daily digest conflated "hours since any send" with "hasn't fired today's routine slot"** — an escalated (attention-tier) send updated the same last-sent timestamp the routine-slot check compared against, so an afternoon escalation could suppress the next morning's routine digest for up to ~20 hours. **Template fix applied 2026-09-01**: added a separate calendar-date marker (`digest-last-routine-date`, owner-local date) that only the routine/overdue triggers write, decoupled from the shared last-sent timestamp used for the escalation-gap and overdue-safety-net logic. Regression test added (`scripts/test/run.sh`). Template-only, not upstream-fileable — this was our own gate script | bug (ours) | fixed in `owner-tldr.sh` 2026-09-01 |
| 32 | **`public-actions.log` written in at least three different formats** (`[intent]/[result]`, `INTENT:/RESULT:`, `intent=...result=...`), and — more seriously than the original consistency gap suggested — the free-form shape let a wall-clock write-time timestamp get mistaken for the actual event's timestamp: a result line logged an hour late (2026-09-XX) made a same-thread GitHub comment look like it was posted *before* the issue it replied to, an apparent impossible ordering that read as a possible identity-compromise incident until checking GitHub's own `created_at` timestamps directly settled it as a logging-clock glitch. Cost a full escalation round-trip to the owner before that check happened. **Fixed 2026-09-01**: defined one canonical JSON-line schema in `instructions.md` with separate `logged_at` (wall-clock write-time, explicitly never authoritative) and `event_time` (the external system's own returned timestamp, the only field usable for ordering claims) fields; added a matching checklist item to `task-integrity.md` — "re-fetch from GitHub and compare its own timestamps before concluding an ordering anomaly means tampering" | bug (ours, was tooling-only, escalated after a real false alarm) | fixed in `instructions.md` + `task-integrity.md` 2026-09-01 |
| 33 | **Discord pipe-style markdown tables (`\| Platform \| Today \|`) don't render** — Discord has no markdown table support at all, renders literal stray pipe characters. Real install caught this on a live metrics post. **Template fix applied 2026-09-03**: `discord-mechanics.md` now documents fenced code blocks as the correct way to post tabular data | bug (docs, ours) | fixed in `discord-mechanics.md` 2026-09-03 |
| 34 | **Release-announcement cards linked the raw GitHub release page, which converts poorly for new-visitor traffic** — owner's own fix, applied live: point the primary card button at the project's marketing landing page (with UTM params) instead, keep the GitHub release notes as a secondary button. Also codified a general UTM-tagging convention for any outbound marketing link the lead posts, not just release cards. **Template fix applied 2026-09-03**: added to `discord-mechanics.md` and `release-announcement-watch.md`, gated on an optional landing-page config value (falls back to the raw GitHub URL if unset) | feature/docs (ours) | fixed in `discord-mechanics.md` + `release-announcement-watch.md` 2026-09-03 |
| 35 | **`ncl tasks update --recurrence` doesn't recompute `process_after`** — changing a task's cadence (e.g. weekly → daily) silently leaves the next scheduled fire on the OLD cadence until `process_after` is separately, manually bumped. Confirmed via source: `updateTaskCommand` treats the two fields as fully independent, no derived recomputation anywhere | bug — `nanocoai/nanoclaw`, `src/cli/resources/tasks.ts` | **filed**: [`nanocoai/nanoclaw#3705`](https://github.com/nanocoai/nanoclaw/issues/3705) |
| 36 | **`ncl groups config add-mount --container <absolute-path>` silently produces a broken, unusable mount.** Corrected root cause (first write-up was wrong): the rejection logic for an absolute `--container` already exists (`isValidContainerPath`/`validateMount` in `src/modules/mount-security/index.ts`) and works correctly — but `add-mount`'s CLI handler never calls it at config-write time, so an invalid mount is stored with zero validation. The check only runs later, at container-spawn time (`validateAdditionalMounts`), and a rejection there produces nothing but a host-side `log.warn` — no error reaches `add-mount`'s own response, `ncl groups get`, or `ncl groups restart`. The mount looks completely valid at every inspection point and simply never exists. Confirmed by direct reproduction on a real install setting up the shared-repo-mirror mount. **Real fix pushed** to `DawoudIO/nanoclaw` branch `fix/add-mount-reject-absolute-container-path` (commit `6df9f417`) — `add-mount` now calls the existing `validateMount` at config-write time, rejecting immediately with a clear reason; includes a regression test and a fix to the existing test that had been exercising this exact bug unknowingly | bug (serious) — `nanocoai/nanoclaw`, `src/cli/resources/groups.ts` | **filed**: [`nanocoai/nanoclaw#3706`](https://github.com/nanocoai/nanoclaw/issues/3706), **fixed on our fork**, not yet PR'd upstream — real fix for tonight's planned mount setup either way |
| 37 | **`unanswered-watch` was structurally blind, not just a wrong-command bug.** Flagged repeatedly since 2026-08-27 as "calls `ncl messages list`, which doesn't exist," but the real root cause was deeper: the local agent was never wired to any support channel at all — only the lead receives inbound support messages, by the one-voice design — so it had zero session history to check regardless of which command it called. Confirmed via source (`src/router.ts`): every wired agent-group gets every message written into its own session (`writeSessionMessage`) unconditionally, regardless of whether its `engage_mode` ever triggers a reply — only the wake-decision side effects are gated, not storage. That means a silent, never-triggering wiring (`--engage-mode mention` on a channel nobody @-mentions "Local Agent" in) genuinely works as a passive observer. **Fixed 2026-09-03**: `welcome/SKILL.md` now silently wires the local agent to every support-tier channel plus a destination per channel to post through; `unanswered-watch.sh` rewritten against `ncl sessions list` + `ncl sessions history` (real commands) instead of the nonexistent one. Manually verified against a mock `ncl` (unanswered/answered/dedup/all-answered paths). Template-only — not upstream-fileable, since the platform behavior (per-group session scoping, unconditional message storage) is working as designed; the bug was entirely in our own wiring instructions and gate script | bug (serious, ours) | fixed in `welcome/SKILL.md` + `unanswered-watch.sh`/`.md` 2026-09-03 |

## Filing checklist (per issue)

- [ ] Reproduced on clean sandbox install (or marked "old install only — may be
      fixed/config-specific" in the issue body)
- [ ] Version/image tag + minimal repro steps + expected vs actual
- [ ] No deployment secrets or private channel names in the issue text
- [ ] Cross-referenced related issues (#411 for anything injection-adjacent)
- [ ] Status column updated here with the filed issue URL
